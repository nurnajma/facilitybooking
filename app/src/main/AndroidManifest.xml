<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FacilityBooking"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Login Activity - LAUNCH ACTIVITY -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Facility List Activity -->
        <activity
            android:name=".FacilityListActivity"
            android:exported="false" />

        <!-- Create Booking Activity -->
        <activity
            android:name=".CreateBookingActivity"
            android:exported="false" />

        <!-- My Bookings Activity -->
        <activity
            android:name=".MyBookingsActivity"
            android:exported="false" />

        <!-- Admin Bookings Activity -->
        <activity
            android:name=".AdminBookingsActivity"
            android:exported="false" />

        <!-- Manage Facilities Activity -->
        <activity
            android:name=".ManageFacilitiesActivity"
            android:exported="false" />

        <!-- Add/Edit Facility Activity -->
        <activity
            android:name=".AddFacilityActivity"
            android:exported="false" />

    </application>

</manifest>